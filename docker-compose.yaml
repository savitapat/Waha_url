version: '3.8'
services:
  waha:
    image: devlikeapro/waha:latest  # ✅ CORRECT
    ports:
      - "3000:3000"  # ✅ CORRECT
    environment:
      - LOG_LEVEL=info  # ✅ CORRECT
    restart: unless-stopped