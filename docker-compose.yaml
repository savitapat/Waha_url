version: '3.8'
services:
  waha:
    image: devlikeapro/waha:latest  # ✅ FIXED: was "deviltesgp"
    ports:
      - "3000:3000"  # ✅ FIXED: was 2000
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped